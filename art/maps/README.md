# Map Art Archive

Use this folder for battle-map image assets or asset metadata when the GM intentionally wants them stored in the repository.

The control record for map art is `tracking/image-output-index.md`, with source and prompt information in `visual-briefs/maps/`.

Battle maps are truth-first. Player-safe and GM-only map variants should be tracked separately.
